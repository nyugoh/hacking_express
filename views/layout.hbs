<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{title}}</title>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/octicons/3.1.0/octicons.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <script src="https://cdn.jsdelivr.net/jquery/2.1.3/jquery.min.js"></script>
    <script src="/js/script.js" charset="utf-8"></script>
  </head>
  <body>
    <div class="container">
      <div class="clearfix header">
        <a href="/"><h3 class="brand">Hacker Weekend</h3></a>
        <div class="btn-group btn-group-sm pull-right">
          <a href="/users/signup" class="btn btn-info">Be a hacker</a>
          <a href="/users" class="btn btn-info">Join a hack</a>
          {{#if currentUser}}
            <a href="/users/logout" class="btn btn-info">Logout</a>
          {{else}}
            <a href="/users/login" class="btn btn-info">Login</a>
          {{/if}}
        </div>
        <div class="clearfix">
          {{#if currentUser}}
            <br><span class="pull-right">
              Hello {{currentUser.username}}
              <div class="dropdown" style="display:inline;">
                <a class="btn dropdown-toggle dropdown-toggle" type="button" data-toggle="dropdown">
                  <span class="octicon octicon-gear text-primary"></span>
                </a>
                <ul class="dropdown-menu">
                  <li><a href="/hacks/{{currentUser.username}}">Your hacks</a></li>
                  <li><a href="/hacks/start/{{currentUser.username}}">Start a hack</a></li>
                  <li><a href="/users/{{currentUser.username}}/edit">Edit Profile</a></li>
                  <li><a href="/users/logout">Logout</a></li>
                </ul>
              </div>
            </span>
          {{/if}}
        </div>
      </div>
      {{#if errors}}
        {{#each errors}}
          <div class="alert alert-danger"><p>{{error}}</p></div>
        {{/each}}
      {{/if}}
      {{#if infos}}
        {{#each infos}}
          <div class="alert alert-danger"><p>{{info}}</p></div>
        {{/each}}
      {{/if}}
      {{{body}}}
      <div class="footer text-center">
        <hr>
        <p>{{appName}}</p>
        <span>Copy&copy; 2017</span>
        <span class="text-success octicon octicon-terminal"></span>
      </div>
    </div>
    <script src="https://cdn.jsdelivr.net/bootstrap/3.3.5/js/bootstrap.min.js"></script>
  </body>
</html>
